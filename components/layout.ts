/**
 * Horizontal gutters.
 *
 * The homepage frame uses 48px; the Work / Blog / Article / About frames all use
 * 120px. Both step down on narrow viewports — the design has no frames below
 * 1440px, so the smaller steps are derived.
 */
export const PAGE_GUTTER = 'px-4 md:px-8 lg:px-30'
export const HOME_GUTTER = 'px-4 md:px-6 lg:px-12'
